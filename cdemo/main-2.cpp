#include <stdio.h>

int main()
{
  int integertype = 3;
  float floattype = 2.5;
  double doubletype = 3.5;
  char charactertype = p;
  short int shortinttype = 5;
  unsigned int unsignedintype = 9;

  printf("Integer size: %d bytes, Float size: %d bytes", integertype, sizeof(floattype));

  return 0;
}